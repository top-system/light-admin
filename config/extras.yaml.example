# ============================================================================
# 扩展功能配置示例
# 将以下内容添加到 config.yaml 中即可启用对应功能
# ============================================================================

# ====== 定时任务配置 ======
# 用于定时执行任务，如数据清理、报表生成等
Crontab:
  Enable: true          # 是否启用
